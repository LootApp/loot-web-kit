## Running locally
`yarn; yarn start`
Starts dev server on port 3000

## Building styleguides website and lib
`yarn run build`

## Requirements
- react
- styled-components
- roboto fonts
- prop types
